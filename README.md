# KRÓDesign site (starter)

This repository contains a minimal Next.js + TypeScript + Tailwind starter scaffold for the KRÓ Design homepage and product categories.

Quick start:

```bash
npm install
npm run dev
```

Visit http://localhost:3000 (Icelandic default). Use `/en` for English.
